template<typename T>
void signal_preprocess(T &signal) {
// this is where just in time iq flipping will occur
    //fprintf(stderr, "I am a %s and my ID is %ld\n", signal->table_name, signal->id);

}
