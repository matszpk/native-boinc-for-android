#include "setilib.h"

// This is here only for completeness.  The bit for this rfi type
// is set "by hand" via a dedicated application.
bool rfi_staff(common_signal_t &signal, std::vector<spike> &spike_field) {
    return(false);
}

